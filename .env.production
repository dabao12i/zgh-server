# Application Port for Production
PORT=8080

# Environment
NODE_ENV=production

# JWT Secret Key (a strong, random string)
JWT_SECRET=your_jwt_secret_key_prod_RANDOM_AND_SECURE

# JWT Token Expiration Time (e.g., 1h, 7d, 24h)
JWT_EXPIRES_IN=1d
