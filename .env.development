# Application Port
PORT=3030

# Environment (development, staging, production)
NODE_ENV=development

# JWT Secret Key (a strong, random string)
JWT_SECRET=your_jwt_secret_key_dev

# JWT Token Expiration Time (e.g., 1h, 7d, 24h)
JWT_EXPIRES_IN=1h

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=gzh_dev
DB_PORT=3306
